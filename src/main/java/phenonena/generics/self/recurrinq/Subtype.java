package phenonena.generics.self.recurrinq;

public class Subtype extends GenericType<Subtype> {
    //just passes itself as a parameter type (defines arg and return value types for methods in GenericType)
    //also inherits all methods from GenericType
}
